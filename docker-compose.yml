version: '3.8'

services:
  dobs-mcp-server:
    build: .
    environment:
      - DOBS_API_KEY=${DOBS_API_KEY}
      - DOBS_BASE_URL=${DOBS_BASE_URL:-https://api.dobs.ai}
    stdin_open: true
    tty: true
    volumes:
      - .:/app
    working_dir: /app